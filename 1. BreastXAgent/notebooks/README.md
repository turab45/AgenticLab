# Notebooks

`breastxagent_original.ipynb` is the original exploratory notebook. The reusable implementation has been extracted into the `breastxagent` Python package under `src/`.

Keep future notebooks focused on experiments, figures, and demonstrations. Shared pipeline code should live in `src/breastxagent` so it can be tested and reused from the CLI.

